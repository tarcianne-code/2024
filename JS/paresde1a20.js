// Usando um laço for para mostrar todos os números pares de 1 a 20
for (let i = 1; i <= 20; i++) {
    if (i % 2 === 0) { // Verifica se o número é par
        console.log(i); // Exibe o número no console
    }
}
